<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <parent>
        <relativePath>../../pom.xml</relativePath>
        <artifactId>airsonic</artifactId>
        <version>11.0.0-SNAPSHOT</version>
        <groupId>org.airsonic.player</groupId>
    </parent>
    <packaging>jar</packaging>
    <artifactId>airsonic-debian</artifactId>
    <name>Airsonic-Advanced-Debian</name> <!-- Becomes the Debian package name -->
    <description>Debian package for Airsonic</description> <!-- Becomes the Debian package description -->

    <properties>
        <debian.user>airsonic</debian.user>
        <debian.group>airsonic</debian.group>
        <debian.dir>/var/airsonic</debian.dir>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.airsonic.player</groupId>
            <artifactId>airsonic-main</artifactId>
            <version>11.0.0-SNAPSHOT</version>
            <type>war</type>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <configuration>
                    <stripClassifier>true</stripClassifier>
                    <stripVersion>true</stripVersion>
                </configuration>
                <executions>
                    <execution>
                        <id>copy-war</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-contrib</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target name="copy">
                                <copy todir="${project.build.directory}/contrib">
                                    <fileset dir="${basedir}/../../contrib" includes="airsonic.service" />
                                </copy>
                                <move file="${project.build.directory}/dependency/airsonic-main.war" tofile="${project.build.directory}/dependency/airsonic.war"/>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.vafer</groupId>
                <artifactId>jdeb</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>jdeb</goal>
                        </goals>
                        <configuration>
                            <verbose>true</verbose>
                            <snapshotExpand>false</snapshotExpand>
                            <snapshotEnv></snapshotEnv>
                            <controlDir>${basedir}/src/deb/control</controlDir>
                            <deb>${project.build.directory}/${debian.install.name}_${project.version}.deb</deb>
                            <dataSet>
                                <data>
                                    <src>${project.build.directory}/dependency/airsonic.war</src>
                                    <type>file</type>
                                    <mapper>
                                        <type>perm</type>
                                        <prefix>${debian.dir}</prefix>
                                        <user>${debian.user}</user>
                                        <group>${debian.group}</group>
                                        <filemode>750</filemode>
                                    </mapper>
                                </data>
                                <data>
                                    <src>${project.build.directory}/contrib</src>
                                    <type>directory</type>
                                    <mapper>
                                        <type>perm</type>
                                        <prefix>${debian.dir}</prefix>
                                        <user>${debian.user}</user>
                                        <group>${debian.group}</group>
                                        <filemode>640</filemode>
                                    </mapper>
                                </data>
                            </dataSet>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
